# This empty file overrides the nginx default.conf (which declares server_name
# localhost on port 80) to prevent a "conflicting server name" warning when
# DOMAIN=localhost. Our app configuration lives in templates/app.conf.template.
