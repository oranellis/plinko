# Development Notes

## Fixes/Requests

- [ ] The backgrounds of several windows and sections of the app are still white or light gray, can you update the task hover status window, the hover of most buttons, the text colour of tags on the tags menu, the chevron arrows in the calendar date select screen.
- [ ] There are some more colour issues I'd like you to fix, the purple and teal colours for the dependents and dependency highlighting is quite dark, can you make it lighter so it's more visible?
- [ ] The milestone border colours are now a bit dark, can you make them brighter?
- [ ] The border colours for selectable and unselectable boxes are swapped, the selectable ones should be brighter. Can you fix this?
- [ ] Can you make the uneditable boxes a bit more obvious? It is now a bit harder to tell with the background a dark gray.

- [ ] Can you update the UI so when I change a task to a status requiring a start or end date that doesn't yet exist, it automatically populates the date with today's date? So editing a task that is in-progress to dropped or complete will put the end date in as today unless changed. Similar but setting the actual start date for tasks going from not started to in-progress
- [ ] Can you have the same logic for clearing actual end dates as clearing actual start dates for tasks which do not have a status which requires an actual end date?
